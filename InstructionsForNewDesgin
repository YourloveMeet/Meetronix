You are redesigning the Meetronix portfolio website from scratch. The existing codebase uses React 19 (Vite), Tailwind CSS v3, Framer Motion v12, Lucide React, and React Router DOM v7.

## NEW DESIGN DIRECTION
The old site was too dark, violet-heavy and generic. The new direction is:
- **Aesthetic**: Warm cream & terracotta — premium, editorial, founder-level
- **Goal**: Convert foreign freelance clients (US, UK, AU, CA) for luxury web projects
- **Feel**: Light and warm base, refined glassmorphism, intentional typography

## NEW DESIGN SYSTEM — Apply globally

### Colors (replace ALL existing colors)
```css
:root {
  --cream: #FAF7F2;
  --cream-dark: #F2EDE4;
  --warm-white: #FFFCF8;
  --terracotta: #C4622D;
  --terra-light: #E8896A;
  --terra-dark: #9A4520;
  --terra-muted: #F0D5C8;
  --charcoal: #1C1917;
  --text-primary: #2C2420;
  --text-secondary: #7A6A62;
  --text-muted: #AFA099;
  --border: rgba(196,98,45,0.12);
  --border-strong: rgba(196,98,45,0.25);
  --glass-bg: rgba(250,247,242,0.72);
  --glass-border: rgba(196,98,45,0.18);
}
```

### Typography
- **Display/Headings**: `'Playfair Display', serif` — weight 400/500, letter-spacing -0.02em
- **Body/UI**: `'DM Sans', sans-serif` — weight 300/400/500
- Import both from Google Fonts in index.html
- All italic text in headings uses the terracotta color (for emphasis words)
- NO Inter, NO Outfit — completely replace

### Glassmorphism (refined version)
Old style was too frosted. New glass:
```css
.glass-card {
  background: rgba(250,247,242,0.72);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(196,98,45,0.18);
  border-radius: 16px;
}
```

### Animated Background (replace AnimatedBackground component)
- NO dark blobs, NO purple/violet
- Warm cream base (#FAF7F2) with very subtle terracotta blobs (opacity 0.06-0.1), large radius, slow movement
- Add a subtle paper texture overlay using CSS noise (opacity 0.03)
- Overall background should feel like premium warm paper

### Buttons
- **Primary**: `background: #C4622D`, white text, border-radius 10px, hover darkens to #9A4520
- **Secondary/Outline**: transparent bg, 1px solid border-strong, text-primary, hover bg: cream-dark
- **Ghost**: no border, text-secondary, hover text-terracotta

### Cards
- Surface: `background: var(--warm-white)`, `border: 1px solid var(--border)`
- Hover: subtle lift — `box-shadow: 0 8px 32px rgba(196,98,45,0.1)`, translateY(-2px)
- border-radius: 14px






📄 Prompt 2 — Navbar
Redesign the Navbar component with these specs:

1. **Logo**: "Meet" in Playfair Display weight 500, "ronix" in terracotta (#C4622D). No icon needed.
2. **Layout**: Logo left, nav links center, CTA button right
3. **Links**: "Work", "About", "Services", "Blog" — DM Sans 13px, text-secondary, active state uses terracotta
4. **CTA button**: "Hire Me →" — terracotta bg, white text, 8px border-radius, 8px 16px padding
5. **Glass effect**: backdrop-blur(20px), background rgba(250,247,242,0.85), 1px solid border, border-radius 14px
6. **Sticky behavior**: on scroll >50px, add subtle box-shadow: 0 4px 24px rgba(44,36,32,0.08)
7. **Mobile**: hamburger menu, slides down as a glass panel
8. Use Framer Motion for the scroll-triggered glass intensity change
9. Remove all dark/violet styling completely




🦸 Prompt 3 — Hero Section
Redesign the Hero section completely:

1. **Layout**: Left-aligned content, right side has a floating project preview card (glass card showing Fauna Rituals or GearGuard)
2. **Availability badge**: Small pill — terracotta dot + "Available for Projects" in terra-muted bg, terra-dark text
3. **Headline**: Playfair Display, 52px desktop / 36px mobile, weight 500
   - Text: "Building premium digital *experiences.*"
   - The italic word "experiences." should be terracotta colored
4. **Subtext**: DM Sans 300, 17px, text-secondary, max-width 480px
   - "Full-stack engineer & designer crafting luxury web products for global brands."
5. **CTA row**: "View My Work →" (primary button) + "Let's Talk" (outline button)
6. **Background**: Warm cream base, two large blobs — terracotta at 6% opacity top-right, and terra-muted at 15% opacity bottom-left, both with blur(80px)
7. **Scroll indicator**: Subtle "scroll" text with animated line pointing down, text-muted color
8. **Framer Motion**: Staggered reveal — badge first, then h1 (0.1s delay), then subtext (0.2s), then CTAs (0.3s), then the right card (0.4s, slides in from right)
9. Remove all existing hero code and replace fully


💼 Prompt 4 — Portfolio / Projects
Redesign the Portfolio page and project cards:

1. **Page header**: "Selected *Work*" — Playfair Display, italic terracotta on "Work"
2. **Filter tabs**: Minimal pill-style tabs — "All", "Web", "SaaS", "Game". Active tab: terracotta bg, white text. Inactive: transparent, text-secondary, hover text-terracotta
3. **Grid**: 3-column desktop, 2-column tablet, 1-column mobile. Gap 20px.
4. **Project card structure**:
   - Image area: 200px height, shows project thumbnail or gradient fallback
   - Top-right badge: glass pill showing project type (Live / SaaS / Demo)
   - Bottom section: project title (Playfair Display 16px), description (DM Sans 12px text-secondary), tech tags (small pills in cream-dark bg)
   - Hover: card lifts (translateY -4px), thin terracotta left border appears (3px), image slightly zooms (scale 1.04)
5. **Project modal**: When opened, shows full-width project image, then 3-column detail cards for "Challenge", "Solution", "Results" — each as a glass card
6. **Tech stack used from projectsData.js** — pull existing data, just change the visual presentation
7. Add a subtle "→ View Case Study" text link that appears on hover at bottom of card




👤 Prompt 5 — About Page
Redesign the About page:

1. **Layout**: Two-column — left is text/story, right is a stacked set of glass cards (stats, skills, current focus)
2. **Opening line**: Large Playfair Display quote-style text — "Engineer. Designer. *Builder.*" with Builder in terracotta italic
3. **Story text**: 2-3 short paragraphs, DM Sans 300, text-secondary, generous line-height
4. **Stats section**: 3 cards in a row — each showing a number (Playfair Display, large) and a label (DM Sans, text-muted, uppercase 10px):
   - "15+" Projects Delivered
   - "4.7yr" Experience
   - "5★" Client Rating
5. **Skills**: Replace the progress bars with a more editorial approach — skill name + a thin terracotta underline that grows on scroll (Framer Motion useInView). Show: React, Next.js, Node.js, TypeScript, AWS, MongoDB, Figma, Unity
6. **Timeline**: Keep the timeline but reskin — vertical line in terra-muted color, year labels in terracotta, content in DM Sans. Year range: 2020 to present.
7. **"Currently" card**: A glass card showing "Currently at AS Technolabs as Full Stack Engineer" with a subtle green dot
8. Remove all dark/violet styling


🛠️ Prompt 6 — Services Page
Redesign the Services page:

1. **Page header**: "What I *Build*" — italic terracotta on "Build"
2. **Service cards**: Large, prominent cards — not small tiles. Each card:
   - Warm white background, 1px border
   - Category label (uppercase, terracotta, 10px, tracking wide)
   - Service title (Playfair Display 22px)
   - Description (DM Sans 300, text-secondary)
   - "Starting from $XXX" pricing hint (optional, subtle)
   - A thin terracotta line that animates in on hover (left border or bottom border)
3. **Services to include**: Luxury Ecommerce, SaaS Development, Web App Design, Unity/Game Dev
4. **Process timeline**: Horizontal scrollable on mobile, static on desktop
   - Steps: Discovery → Design → Build → Test → Launch
   - Each step: terracotta circle number, step name, 1-line description
   - Connecting line in terra-muted
5. **CTA strip at bottom**: Glass card — "Ready to build something exceptional?" with a primary CTA button


✍️ Prompt 7 — Blog Page
Redesign the Blog page:

1. **Page header**: "Thoughts & *Insights*" — Playfair Display
2. **Search bar**: Warm white bg, 1px border, rounded-full, search icon in terracotta, focus ring in terracotta
3. **Category pills**: Same style as portfolio filters — terracotta active state
4. **Article cards**:
   - Warm white card, 1px border, 14px border-radius
   - Top: category badge (terra-muted bg, terra-dark text)
   - Title: Playfair Display 16px
   - Excerpt: DM Sans 13px text-secondary, 2 lines max
   - Footer: read time + date (DM Sans 11px text-muted) + "Read →" link that animates on hover
5. **Newsletter section**: Full-width glass card — "Get occasional insights on design and development." — email input + subscribe button
6. Remove all dark/violet styling




⚙️ Prompt 8 — Global Cleanup
Final cleanup pass across all components:

1. **index.css / tailwind config**:
   - Add all CSS custom properties from the design system
   - Add `.animate-blob` with the new warm color keyframes
   - Add `.glass-card` utility class
   - Remove all slate/violet/purple color references

2. **Google Fonts**: In index.html, add:
   <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,500;0,600;1,400;1,500&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">

3. **PageTransitions**: Keep existing Framer Motion logic, just ensure background during transition is cream not dark

4. **ScrollToTop**: No visual change needed

5. **Meta / SEO**: Update page title to "Meet Patel — Full Stack Engineer & Designer | Meetronix" and description to target international clients

6. **Favicon**: If possible, generate a simple "M" monogram in terracotta on cream background as SVG favicon

7. **Remove entirely**: Any remaining bg-slate-*, text-violet-*, border-purple-*, or bg-dark references across all files


AND DON'T TEST ANYTHING IN THE BROWSER , I WILL CHECK IT MYSELF. AND GO WITH ONE BY ONE PROPERLY